---
- name: Test if the postgres is running
  shell: psql -c "SELECT version();"
  args:
    executable: /bin/bash
  register: postgresql_version
  become: yes
  become_user: postgres
  become_method: sudo
  changed_when: False
  failed_when:
    - '"PostgreSQL 10" not in postgresql_version.stdout'